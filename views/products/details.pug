extends ../layouts/common.pug

block content
    .row.mt-5
        .col-md-4 
            img(src= product.image style="width:100%")
        .col-md-8 
            h1 #{product.name}
            p #{product.description}
